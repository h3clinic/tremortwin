# Code map

Auto-generated by `gen_codemap.py` from the abstract syntax tree, so it
always matches the code. Every module, class and function in the
repository is listed below, in dependency order (foundation first).

Total: 16 Python files, 2548 lines of Python.

Regenerate with:

```bash
python gen_codemap.py
```

## Dependency order

```
twin.py            (no internal deps)
  -> generator.py  (imports twin)
  -> features.py   (imports twin)
       -> selfimprove.py, leakage.py   (pure sklearn/numpy)
            -> run.py            (imports generator, features, selfimprove, leakage)
            -> train_export.py   (imports generator, features, twin)
                 -> bridge_selfcheck.py, predict_on_video.py
       -> realdata_uci395.py, pads_validation.py  (import features only)
       -> make_figures.py, make_pgf.py            (import everything, read-only)
```

### `twin.py`

**Role.** Foundation. Forward-kinematics rig + tremor forward model.

*396 lines.* Landmark-level digital twin of the handharness Blender tremor pipeline.

Module constants: `HERE`, `_ASSET`, `DEFAULT_GLB`, `LANDMARK_ORDER`, `FINGER_CHAINS`, `WRIST`, `CLASS_NAMES`, `CLASS_IDX`, `VIEWS`

| Definition | Purpose | Line |
|---|---|---|
| `_find_glb()` | Locate the rigged-hand GLB. | 39 |
| `_quat_to_mat(q)` | glTF quaternion [x, y, z, w] -> 3x3 rotation matrix. | 85 |
| `_euler_to_mat(rx, ry, rz)` | Euler XYZ -> 3x3. Small-angle regime (tremor < 0.15 rad), so the precise | 101 |
| `_mat4(R=..., t=...)` | -- | 113 |
| class `RigNode` | -- | 123 |
| class `HandRig` | Forward-kinematics skeleton parsed straight from the GLB. | 132 |
| &nbsp;&nbsp;`__init__(self, glb_path=...)` | -- | 135 |
| &nbsp;&nbsp;`_read_gltf_nodes(glb_path)` | -- | 157 |
| &nbsp;&nbsp;`_topo_order(self)` | -- | 173 |
| &nbsp;&nbsp;`fk(self, extra_rot=...)` | Compute world-space origins for every node. | 181 |
| &nbsp;&nbsp;`landmarks(self, extra_rot=...)` | Return (21, 3) array in LANDMARK_ORDER. | 200 |
| `tremor_params_coupled(intensity)` | EXACT mapping from scripts/blender_tremor_sequence.py tremor_params(). | 211 |
| `clinical_bin(intensity)` | EXACT 6-band clinical mapping from the repo (UPDRS / TETRAS references). | 225 |
| `wrist_tremor_euler(amp_rad, freq_hz, t, rng)` | Per-frame wrist Euler rotation (rad). Matches compute_tremor_rotation(): | 245 |
| `random_static_pose(rig, rng)` | Sample a static finger configuration (held fixed across a clip), mirroring | 258 |
| `_view_matrix(azimuth_deg, elevation_deg)` | -- | 290 |
| `project(landmarks3d, azimuth, elevation, wrist_idx=...)` | Weak-perspective projection of (21,3) -> (21,2) normalized image coords. | 300 |
| class `ObsNoise` | MediaPipe observation-noise model (in normalized image units). | 315 |
| `add_observation_noise(traj, rng, noise)` | traj: (T, 21, 2) -> noised copy. Dropouts are forward-filled (as a real | 323 |
| `render_clip(rig, pose_extra, amp_rad, freq_hz, n_frames, fps, view, rng, noise, phase_offset=...)` | Produce one (T, 21, 2) landmark trajectory for a pose+tremor+view. | 343 |

### `generator.py`

**Role.** Dataset synthesis (coupled and decoupled label models).

*169 lines.* Dataset generators built on the validated landmark twin.

Module constants: `AMP_EDGES`, `SEVERITY_BY_AMP`, `TREMOR_TYPES`, `TYPE_IDX`

| Definition | Purpose | Line |
|---|---|---|
| `severity_from_amp(amp_rad)` | -- | 47 |
| class `Subject` | -- | 57 |
| class `Clip` | -- | 64 |
| `make_subjects(n, rng)` | -- | 78 |
| `_apply_subject_shape(rig, shape_scale)` | Return a per-subject FK closure: landmarks scaled anisotropically about | 92 |
| `_render_clip_subject(rig, lm_fn, pose_extra, amp_rad, freq_hz, n_frames, fps, view, rng, noise, phase_offset)` | Like twin.render_clip but uses a subject-shaped landmark function. | 102 |
| `generate(mode, n_poses, n_frames, fps, n_subjects, seed, views=..., p_no_tremor=...)` | Yield Clip objects. One pose -> one tremor state, rendered from each view. | 117 |

### `features.py`

**Role.** Blind feature extraction from landmark trajectories.

*200 lines.* Blind feature extraction from 2D landmark trajectories.

Module constants: `_HP_CUT`, `WRIST_I`, `MID_BASE_I`, `FINGERTIPS`, `BANDS`

| Definition | Purpose | Line |
|---|---|---|
| `_highpass(sig, fs, cut=...)` | -- | 33 |
| `_hand_scale(traj)` | Mean wrist->middle-metacarpal distance over the clip (normalized units). | 51 |
| `_peak_freq(sig, fps)` | Welch PSD dominant frequency with parabolic sub-bin interpolation, plus | 57 |
| `_temporal(disp, fps)` | Temporal descriptors of a 1D displacement-magnitude signal. | 99 |
| `extract(traj, fps)` | traj (T,21,2) -> flat feature dict. Pure function of the landmark stream. | 122 |
| `feature_groups(all_keys)` | -- | 192 |

### `selfimprove.py`

**Role.** The self-improving feature/model search and metrics.

*198 lines.* The self-improvement ML loop.

| Definition | Purpose | Line |
|---|---|---|
| `clf_metrics(y_true, y_pred)` | -- | 37 |
| `reg_metrics(y_true, y_pred)` | -- | 46 |
| `make_model(task, name, hp, seed=...)` | -- | 61 |
| `_grouped_cv_score(task, model_spec, X, y, groups, primary, seed=..., n_splits=...)` | -- | 97 |
| `_round_configs(round_idx, task, best_family)` | Feature-set escalation + candidate model configs for this round. | 115 |
| `select_features(feature_names, groups_active, feature_groups_map)` | -- | 148 |
| `run_self_improvement(Xfull, y, groups, feature_names, feature_groups_map, task, primary, target, max_rounds=..., patience=..., seed=..., log=...)` | Iterative search. Returns history + winning model refit on all train data. | 156 |

### `leakage.py`

**Role.** Data-leakage audit suite.

*118 lines.* Data-leakage audit suite.

| Definition | Purpose | Line |
|---|---|---|
| `group_disjoint(*group_arrays)` | -- | 35 |
| `label_collinearity(freq, severity_idx, amp)` | -- | 45 |
| `label_shuffle_null(X, y, groups, task, seed=...)` | Grouped-CV score with labels shuffled. Should fall to chance. | 62 |
| `train_test_separability(X_train, X_test, seed=...)` | Domain classifier: can a model distinguish train from test rows? | 94 |
| `permutation_top(model, X_test, y_test, feature_names, scoring=..., k=..., seed=...)` | -- | 113 |

### `run.py`

**Role.** Orchestrator: generate -> features -> search -> audit -> JSON.

*175 lines.* Orchestrate the full experiment for one generator mode.

Module constants: `OUT`

| Definition | Purpose | Line |
|---|---|---|
| `build_matrix(mode, poses, frames, fps, subjects, seed, p_no_tremor)` | -- | 32 |
| `subject_holdout(subject_id, test_frac, seed)` | -- | 56 |
| `run_mode(mode, poses, frames, fps, subjects, seed, test_frac, targets, p_no_tremor)` | -- | 63 |
| `main()` | -- | 139 |

### `make_report.py`

**Role.** Turns result JSON into RESULTS_REPORT.md and fills the paper notes.

*124 lines.* Build RESULTS_REPORT.md and fill IEEE_HEALTHCOM_NOTES.md placeholders from

Module constants: `HERE`, `R`

| Definition | Purpose | Line |
|---|---|---|
| `load(mode)` | -- | 11 |
| `pct(x)` | -- | 15 |
| `main()` | -- | 19 |

### `train_export.py`

**Role.** Trains on all synthetic data and pickles the models.

*69 lines.* Train the frequency + severity models on ALL synthetic (decoupled) data and

Module constants: `OUT`

| Definition | Purpose | Line |
|---|---|---|
| `build(poses, frames, fps, subjects, seed)` | -- | 26 |
| `main()` | -- | 41 |

### `bridge_selfcheck.py`

**Role.** Validates the saved-model inference path without MediaPipe.

*60 lines.* Validate the saved-model inference path WITHOUT MediaPipe.

Module constants: `M`

| Definition | Purpose | Line |
|---|---|---|
| `main()` | -- | 25 |

### `predict_on_video.py`

**Role.** Sim-to-real bridge: real video -> MediaPipe -> prediction.

*167 lines.* Sim-to-real bridge: apply the synthetic-trained models to REAL hand videos.

| Definition | Purpose | Line |
|---|---|---|
| `landmarks_from_video(path, model_path, max_frames=..., min_detect=...)` | Return (T,21,2) normalized landmark trajectory + detection rate, or None. | 33 |
| `load_labels(path)` | -- | 80 |
| `main()` | -- | 91 |

### `realdata_uci395.py`

**Role.** Real-tremor validation on UCI-395 pen trajectories.

*139 lines.* Real-tremor validation of our spectral frequency core on UCI-395.

Module constants: `ZIP`, `STCP`

| Definition | Purpose | Line |
|---|---|---|
| `highpass(sig, fs, cut=...)` | Remove postural drift/sway (<cut Hz) so peak-picking sees the tremor, not | 32 |
| `parse_stcp(raw)` | Return (t_seconds, X, Y) for the TestID=2 segment, or None. | 45 |
| `analyze(t, x, y)` | Resample to uniform fs, run the spectral core on X and Y, return descriptors. | 65 |
| `main()` | -- | 88 |

### `pads_validation.py`

**Role.** Real-tremor validation on the PADS accelerometer cohort.

*128 lines.* Real-tremor validation on PADS (PhysioNet Parkinson's Disease Smartwatch).

Module constants: `ZIP`, `R`, `Z`, `NAMES`

| Definition | Purpose | Line |
|---|---|---|
| `conditions()` | -- | 37 |
| `descriptor(pid, task)` | Best-wrist tremor descriptor for a patient+task, or None. | 46 |
| `auc(pos, neg, key)` | -- | 74 |
| `main()` | -- | 81 |

### `make_figures.py`

**Role.** Renders the paper figures as PDFs from the real model and data.

*178 lines.* Generate the paper figures from the actual twin, models, and real datasets.

Module constants: `HERE`, `FIG`, `IDX`

| Definition | Purpose | Line |
|---|---|---|
| `fig_twin()` | Panel A: projected 21-landmark hand skeleton. Panel B: fingertip tremor trace. | 31 |
| `fig_collinearity()` | Frequency vs amplitude for coupled (a line) vs decoupled (a cloud). | 63 |
| `fig_freq_scatter()` | Predicted vs true frequency on held-out synthetic clips (trained model). | 87 |
| `fig_realdata()` | (a) UCI-395 recovered PD-rest freq raw vs high-pass. (b) PADS freq by group. | 115 |

### `make_pgf.py`

**Role.** Emits the self-contained paper with figures as inline pgfplots.

*199 lines.* Emit a fully self-contained paper .tex in which every figure is native

Module constants: `HERE`, `IDX`

| Definition | Purpose | Line |
|---|---|---|
| `coords(xs, ys, prec=...)` | -- | 22 |
| `twin_tikz()` | -- | 27 |
| `collinearity_tikz()` | -- | 67 |
| `freq_tikz()` | -- | 90 |
| `real_tikz()` | -- | 116 |
| `main()` | -- | 180 |

### `probe_glb.py`

**Role.** One-off: inspects the rigged-hand asset (joints, skins, meshes).

*85 lines.* Probe the rigged hand GLB to recover joint/landmark rest geometry.

Module constants: `GLB`

| Definition | Purpose | Line |
|---|---|---|
| `probe_trimesh()` | -- | 16 |
| `probe_pygltflib()` | Read raw glTF JSON for node hierarchy + skin joints (no mesh needed). | 39 |

### `gen_codemap.py`

**Role.** This file. Regenerates docs/CODE_MAP.md from the AST.

*143 lines.* Regenerate docs/CODE_MAP.md by parsing every Python file in the repository.

Module constants: `HERE`, `OUT`, `ORDER`, `ROLE`

| Definition | Purpose | Line |
|---|---|---|
| `sig(node)` | -- | 45 |
| `first_line(doc)` | -- | 61 |
| `describe(path)` | -- | 70 |
| `main()` | -- | 102 |
